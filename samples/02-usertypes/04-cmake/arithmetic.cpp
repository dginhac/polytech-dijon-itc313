#include "arithmetic.h"

// Code of the maxOfTwo function
int max_of_two(int x, int y) {
    if (x > y) return x;
    else return y;
}

// Code of the isEven function
bool is_even(int x) {
    return x % 2 == 0;
}
