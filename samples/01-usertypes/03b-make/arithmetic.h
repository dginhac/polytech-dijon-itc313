// Include guards prevent multiple inclusions:
// they ensure each header is compiled only once
#ifndef ARITHMETIC_H
#define ARITHMETIC_H

// Function prototypes - only the function signature (return tyoe, name, parameters)
int maxOfTwo(int x, int y);
bool isEven(int x);

#endif // ARITHMETIC_H
